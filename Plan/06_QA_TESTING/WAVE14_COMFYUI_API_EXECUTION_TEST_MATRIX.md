# ComfyUI API Execution Test Matrix

Dry-run tests produce request/result manifests without network calls.

Runtime tests require /object_info, /system_stats, /prompt, /history/{prompt_id}, output resolution, and QA scoring.
