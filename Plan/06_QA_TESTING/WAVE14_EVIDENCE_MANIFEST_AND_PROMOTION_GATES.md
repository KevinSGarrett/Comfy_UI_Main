# Evidence Manifest and Promotion Gates

Each pass records run_id, pass_id, attempt, workflow hash, patch hash, engine route, prompt/seed, source artifacts, prompt_id, history, outputs, QA decisions, and rerun decisions.

Promotion requires all required passes to pass.
