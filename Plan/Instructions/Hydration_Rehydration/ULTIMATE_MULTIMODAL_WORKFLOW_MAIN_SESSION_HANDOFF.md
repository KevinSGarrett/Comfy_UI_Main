# Ultimate Multimodal Workflow Main-Session Handoff

Updated: 2026-07-16 America/Chicago

## Main task

Task 019f422f-88b1-7382-872b-21de2089e983 is the implementation authority for the primary ComfyUI project. This handoff reserves an additive Wave64 planning package for Rows149-220.

## Preservation rule

Treat every file listed by the Wave64 control-package registry as intentional project work. Do not delete, clean, overwrite, or absorb it into unrelated FLUX.2 evidence merely because it is untracked or dirty. Rows001-148, their sidecars, current accepted artifacts, and active FLUX.2 work remain unchanged.

The exact path/hash inventory is `Plan/Instructions/Hydration_Rehydration/WAVE64_ULTIMATE_MULTIMODAL_WORKFLOW_PRESERVATION_MANIFEST.json`. Review that manifest and record adoption before staging, renaming, or implementing the package.

## Architectural decision to carry forward

Use one external durable control plane over many small versioned ComfyUI API workflows. Select an exact engine/model/workflow/runtime stack independently for every pass. Hard-filter eligibility before ranking. Move between engine families through decoded artifact bridges. Permit certified specialist engines for regional/body-part/detail passes while protecting accepted parents, other characters, and the full artifact. Use App Mode as the operator façade, not the reasoning/state engine.

## MaskFactory dependency

MaskFactory task 019f4cfc-60c3-7500-8626-261dcf70db5d remains a separate authority. Mode A is package access; Mode B is live prediction/refinement access. Truth tier and certificate are separate. Main must consume immutable masks through a binding crosswalk and must not mutate or relabel MaskFactory gold/drafts.

## Integration sequence after review

1. validate the new schemas, registries, examples, and Rows149-220 sidecars;
2. parameterize accepted Character 1 work into a Character Package publisher without rerunning accepted outputs;
3. implement single- and two-character Shot/Pose/ownership fixtures;
4. implement capability routing, decoded bridges, and specialist-pass contracts;
5. implement MaskFactory Mode A adapter before relying on live Mode B drafts;
6. replace the static pass list and increment-seed retry behavior with an event-backed DAG and material-hypothesis repair;
7. prove single-character, two-character/contact, video-span, audio-stem, and AV synchronization paths in that order;
8. build the App Mode surface over the proven controller;
9. benchmark self-hosted planner/reviewer candidates only after deterministic validators and tool boundaries exist.

## Status truth

This package establishes extensive planning and machine-checkable contracts only. It does not claim installed models, working workflows, MaskFactory service availability, runtime quality, promoted engines, or completed Rows149-220. Each claim advances only through row-specific evidence.
