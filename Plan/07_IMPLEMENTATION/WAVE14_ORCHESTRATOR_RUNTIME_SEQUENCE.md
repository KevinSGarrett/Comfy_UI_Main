# Orchestrator Runtime Sequence

operator request → scene plan → run plan → preflight → patch → dry-run or execute → prompt_id → history/output evidence → QA → rerun/fallback/stop → next pass → promotion manifest.
