# Wave 14 Implementation Manual

Dry-run path:
compile plan → validate plan → inventory patch targets → patch workflow copy → build dry-run execution result → score evidence → create rerun plan if needed.

Execution path adds ComfyUI preflight, /prompt submission, history/output collection, and pass-specific QA.
