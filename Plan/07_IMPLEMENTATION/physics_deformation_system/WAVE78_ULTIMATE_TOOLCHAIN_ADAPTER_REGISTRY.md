# Wave78 Ultimate Toolchain Adapter Registry

Purpose: Register every tool and plugin discussed for the future physics/deformation system as an autonomous adapter, offline asset source, optional backend, or blocked dependency.

Activation gate: Deferred. Activate only after the current ComfyUI foundation, Wave70 Mask Factory, Waves71-76 base physics/deformation layers, runtime lanes, cost controls, and strict QA gates are stable enough that this expansion will not derail nearer project milestones. Activation requires an explicit source-cited project decision.

This wave is intentionally deferred. It is included so the autonomous project has exact end-to-end planning, Items, Tracker rows, QA gates, and future implementation instructions. It must not displace nearer active ComfyUI runtime, workflow, cost-control, Mask Factory, or current-lane work.

DAZ boundary: DAZ is only a neutral A-pose or T-pose prototype source. After prototype registration, DAZ must not be used for runtime morphing, grip posing, hand posing, dForce simulation, production rigging, or production deformation. Blender, Maya, Houdini, Unreal, Marvelous, Substance, ComfyUI, and validated adapters own the autonomous downstream pipeline.

Autonomy rule: after any user-supplied DAZ prototype/reference asset is registered, all setup, conversion, adapter execution, map generation, routing, testing, QA, visual review, video review, audio review, correction, rerun, tracker update, and blocker recording must be autonomous.

Strict QA rule: no row can be promoted from documentation, taxonomy presence, a single smoke artifact, a model review without deterministic evidence, or target-region-only review. Generated outputs require whole-artifact visual review and, for video/audio work, full-duration temporal/audio review.

Evidence-first supervisor rule: LLM/VLM review can recommend, route, and interpret, but it cannot override failed deterministic metrics for topology, geometry, maps, collision, support contact, audio sync, or whole-artifact QA.

External references:

- AWS G7e production GPU runtime: https://aws.amazon.com/ec2/instance-types/g7e/ -- Production self-hosted LLM/VLM and heavy adapter execution target; use with explicit runtime windows and cost controls.
- vLLM model serving: https://docs.vllm.ai/en/latest/models/supported_models/ -- Candidate high-throughput self-hosted inference server for supervisor, code, and multimodal models.
- Qwen VL family: https://github.com/QwenLM/Qwen2.5-VL -- Candidate self-hosted visual review model family for overlays, frame grids, maps, and generated artifacts.
- Blender command line: https://docs.blender.org/manual/en/latest/advanced/command_line/arguments.html -- Primary autonomous production fitting, rigging, map baking, support-surface, and overlay backend.
- SideFX Houdini batch workflow: https://www.sidefx.com/docs/houdini/render/batch.html -- Advanced procedural, Vellum, SDF, pressure, contact, and tissue simulation backend.
- Unreal Python automation: https://dev.epicgames.com/documentation/unreal-engine/scripting-the-unreal-editor-using-python -- Realtime proof, Control Rig, Chaos, Deformer Graph, ML Deformer, and render-pass automation backend.
- Marvelous Designer Python API: https://developer.marvelousdesigner.com/ -- Garment, blanket, sheet, fabric tension, cloth stretch, and cloth/body collision backend.
- Substance Automation Toolkit: https://adobedocs.github.io/substance-automation-toolkit/ -- Batch material, normal, roughness, displacement, curvature, AO, pressure, wrinkle, fabric, and tension map baking.
- Maya command line and Python: https://help.autodesk.com/view/MAYAUL/2026/ENU/ -- Production rigging, skinning, blendshape, pose-space deformation, muscle, Bifrost, and validation backend.
- MotionBuilder Python SDK: https://help.autodesk.com/view/MOBPRO/2026/ENU/ -- Mocap, retargeting, HumanIK, characterization, takes, story, relation constraints, and batch FBX animation handling.
- Cascadeur Python scripting: https://cascadeur.com/help -- Optional physics-aware animation cleanup, balance, AutoPosing, AutoPhysics, contact timing, and motion plausibility backend.
- ZBrush ZScript: https://help.maxon.net/zbr/en-us/Content/html/user-guide/customizing-zbrush/zscripting/command-reference/command-reference.html -- Offline/source asset creation for universal base sculpt, alphas, pores, wrinkles, displacement, and corrective sculpt references.

## Core Runtime Tool Adapters

Source key prefix: W78:core_runtime_tool_adapters

Completion rule: every row in this section remains Deferred_Required_Not_Complete until activation gate, implementation artifacts, adapter evidence, preview/overlay evidence, strict QA, and promotion evidence all pass.

| requirement | implementation_target | autonomous_behavior | acceptance_summary |
| --- | --- | --- | --- |
| DAZ neutral prototype export adapter | daz_neutral_prototype_export_adapter | Register daz_neutral_prototype_export_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | daz_neutral_prototype_export_adapter passes only when the autonomous adapter proves the tool can perform neutral A/T-pose prototype export only, supports source scene, FBX/OBJ, textures, reference renders, hashes, and no runtime DAZ deformation, and produces machine-readable evidence without human intervention after registration. |
| Diffeomorphic DAZ import adapter | diffeomorphic_daz_import_adapter | Register diffeomorphic_daz_import_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | diffeomorphic_daz_import_adapter passes only when the autonomous adapter proves the tool can perform optional DAZ-to-Blender import route, supports Genesis geometry import, materials, metadata, and reference conversion without production rig authority, and produces machine-readable evidence without human intervention after registration. |
| Blender core background adapter | blender_core_background_adapter | Register blender_core_background_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | blender_core_background_adapter passes only when the autonomous adapter proves the tool can perform primary production fitting, rigging, maps, support surfaces, overlays, and exports, supports Python background mode, geometry nodes, simulation nodes, cloth, soft body, shape keys, drivers, armature, mesh deform, surface deform, lattice, data transfer, Alembic, USD, and FBX, and produces machine-readable evidence without human intervention after registration. |
| Houdini FX batch adapter | houdini_fx_batch_adapter | Register houdini_fx_batch_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | houdini_fx_batch_adapter passes only when the autonomous adapter proves the tool can perform advanced procedural/SDF/Vellum/tissue simulation, supports hython/hbatch, Vellum, SDF fields, KineFX, PDG/TOPs, HQueue, Solaris, and USD, and produces machine-readable evidence without human intervention after registration. |
| Unreal Engine Python adapter | unreal_engine_python_adapter | Register unreal_engine_python_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | unreal_engine_python_adapter passes only when the autonomous adapter proves the tool can perform realtime proof and rendered pass backend, supports Control Rig, IK Rig/Retargeter, Sequencer, Chaos Cloth, Panel Cloth, Chaos Flesh, Deformer Graph, ML Deformer, geometry cache, Alembic, USD, and Movie Render Queue, and produces machine-readable evidence without human intervention after registration. |
| Marvelous Designer CLO adapter | marvelous_clo_python_adapter | Register marvelous_clo_python_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | marvelous_clo_python_adapter passes only when the autonomous adapter proves the tool can perform garment, blanket, sheet, fabric pressure, cloth/body collision, and drape simulation, supports Python/plugin automation, garment physical properties, Alembic, FBX, OBJ, and USD export, and produces machine-readable evidence without human intervention after registration. |
| ComfyUI physics conditioning adapter | comfyui_physics_conditioning_adapter | Register comfyui_physics_conditioning_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | comfyui_physics_conditioning_adapter passes only when the autonomous adapter proves the tool can perform final image/video/audio conditioning and generation, supports ControlNet, IPAdapter, inpaint/detailer, video nodes, masks, depth, normal, segmentation, optical flow, and QA manifests, and produces machine-readable evidence without human intervention after registration. |

## Blender Addon Tool Adapters

Source key prefix: W78:blender_addon_tool_adapters

Completion rule: every row in this section remains Deferred_Required_Not_Complete until activation gate, implementation artifacts, adapter evidence, preview/overlay evidence, strict QA, and promotion evidence all pass.

| requirement | implementation_target | autonomous_behavior | acceptance_summary |
| --- | --- | --- | --- |
| Auto-Rig Pro adapter | auto_rig_pro_adapter | Register auto_rig_pro_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | auto_rig_pro_adapter passes only when the autonomous adapter proves the tool can perform production rigging, retargeting, and export, supports autonomous rig generation checks, bone naming, retarget, FBX, and GLTF, and produces machine-readable evidence without human intervention after registration. |
| Voxel Heat Diffuse Skinning adapter | voxel_heat_diffuse_skinning_adapter | Register voxel_heat_diffuse_skinning_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | voxel_heat_diffuse_skinning_adapter passes only when the autonomous adapter proves the tool can perform automatic skin weighting for complex or overlapping meshes, supports skin-weight pass/fail overlays and deformation smoke evidence, and produces machine-readable evidence without human intervention after registration. |
| Faceform R3DS Wrap adapter | faceform_r3ds_wrap_adapter | Register faceform_r3ds_wrap_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | faceform_r3ds_wrap_adapter passes only when the autonomous adapter proves the tool can perform high-quality non-rigid fitting of universal topology to DAZ/reference shape, supports landmark wrap, topology preservation, distance heatmaps, and fit overlays, and produces machine-readable evidence without human intervention after registration. |
| RetopoFlow fallback adapter | retopoflow_fallback_adapter | Register retopoflow_fallback_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | retopoflow_fallback_adapter passes only when the autonomous adapter proves the tool can perform manual/semi-auto retopology fallback only when stable topology cannot be fit, supports blocked-by-human policy unless automated path exists, and produces machine-readable evidence without human intervention after registration. |
| Quad Remesher fallback adapter | quad_remesher_fallback_adapter | Register quad_remesher_fallback_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | quad_remesher_fallback_adapter passes only when the autonomous adapter proves the tool can perform fast quad retopo fallback for non-production proxy assets, supports proxy-only topology report and no replacement of universal production mesh, and produces machine-readable evidence without human intervention after registration. |
| Faceit adapter | faceit_adapter | Register faceit_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | faceit_adapter passes only when the autonomous adapter proves the tool can perform facial shape keys, expression controls, facial mocap or validation, supports identity-safe face region controls and expression QA, and produces machine-readable evidence without human intervention after registration. |
| KeenTools FaceBuilder FaceTracker adapter | keentools_facebuilder_facetracker_adapter | Register keentools_facebuilder_facetracker_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | keentools_facebuilder_facetracker_adapter passes only when the autonomous adapter proves the tool can perform head/face reconstruction or tracking reference, supports face landmark, identity, and fit evidence, and produces machine-readable evidence without human intervention after registration. |
| Cloth Weaver Simply Cloth adapter | cloth_weaver_simply_cloth_adapter | Register cloth_weaver_simply_cloth_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | cloth_weaver_simply_cloth_adapter passes only when the autonomous adapter proves the tool can perform fast cloth prototype and cloth setup inside Blender, supports cloth setup manifest and collision proof, and produces machine-readable evidence without human intervention after registration. |
| Wiggle Bones adapter | wiggle_bones_adapter | Register wiggle_bones_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | wiggle_bones_adapter passes only when the autonomous adapter proves the tool can perform spring/jiggle bone prototyping, supports secondary motion approximation manifest and QA, and produces machine-readable evidence without human intervention after registration. |
| Ragdoll Dynamics Blender adapter | ragdoll_dynamics_blender_adapter | Register ragdoll_dynamics_blender_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | ragdoll_dynamics_blender_adapter passes only when the autonomous adapter proves the tool can perform ragdoll and physics transform testing, supports contact, balance, and motion plausibility evidence, and produces machine-readable evidence without human intervention after registration. |

## Studio Grade Specialist Tool Adapters

Source key prefix: W78:studio_grade_specialist_tool_adapters

Completion rule: every row in this section remains Deferred_Required_Not_Complete until activation gate, implementation artifacts, adapter evidence, preview/overlay evidence, strict QA, and promotion evidence all pass.

| requirement | implementation_target | autonomous_behavior | acceptance_summary |
| --- | --- | --- | --- |
| Maya mayapy production adapter | maya_mayapy_production_adapter | Register maya_mayapy_production_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | maya_mayapy_production_adapter passes only when the autonomous adapter proves the tool can perform studio rigging, skinning, blendshape, pose-space deformation, muscle, Bifrost, and validation, supports mayabatch/mayapy, OpenMaya, HumanIK, Time Editor, Animation Layers, skinCluster, blendShape, Delta Mush, Pose Editor, nCloth, nHair, Maya Muscle, and Bifrost, and produces machine-readable evidence without human intervention after registration. |
| Maya plugin suite adapter | maya_plugin_suite_adapter | Register maya_plugin_suite_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | maya_plugin_suite_adapter passes only when the autonomous adapter proves the tool can perform production rig and deformation plugins, supports mGear, Advanced Skeleton, ngSkinTools2, SHAPES, weightDriver, RBF Solver, Ragdoll Dynamics, Zoo Tools Pro, Maya Bonus Tools, and optional Faceware, and produces machine-readable evidence without human intervention after registration. |
| MotionBuilder retargeting adapter | motionbuilder_retargeting_adapter | Register motionbuilder_retargeting_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | motionbuilder_retargeting_adapter passes only when the autonomous adapter proves the tool can perform mocap retargeting, characterization, cleanup, and batch FBX processing, supports pyfbsdk, Open Reality SDK, FBX SDK, HumanIK, Actor, Control Rig, Story, Takes, and Relation Constraints, and produces machine-readable evidence without human intervention after registration. |
| Cascadeur physics animation adapter | cascadeur_physics_animation_adapter | Register cascadeur_physics_animation_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | cascadeur_physics_animation_adapter passes only when the autonomous adapter proves the tool can perform physics-aware animation cleanup and motion plausibility, supports AutoPosing, AutoPhysics, secondary motion, Python API, FBX/DAE export, and custom rig templates, and produces machine-readable evidence without human intervention after registration. |
| Substance 3D automation adapter | substance_3d_automation_adapter | Register substance_3d_automation_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | substance_3d_automation_adapter passes only when the autonomous adapter proves the tool can perform batch material and texture map generation, supports Painter, Designer, Sampler, Automation Toolkit, sbsbaker, sbscooker, sbsrender, sbsmutator, export presets, UDIM, OCIO, smart materials, and smart masks, and produces machine-readable evidence without human intervention after registration. |
| ZBrush offline asset source registry | zbrush_offline_asset_source_registry | Register zbrush_offline_asset_source_registry as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | zbrush_offline_asset_source_registry passes only when the autonomous adapter proves the tool can perform offline sculpt/detail source, not required unattended runtime, supports ZScript, GoZ, ZWrap, ZRemesher, Decimation Master, Multi Map Exporter, UV Master, Morph Targets, Layers, Polygroups, Project All, Surface Noise, alphas, brushes, pores, wrinkles, and corrective sculpt references, and produces machine-readable evidence without human intervention after registration. |
| Mocap source adapter registry | mocap_source_adapter_registry | Register mocap_source_adapter_registry as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | mocap_source_adapter_registry passes only when the autonomous adapter proves the tool can perform optional mocap ingestion and retarget source handling, supports Rokoko, Xsens, Move.ai, OptiTrack, Vicon, Reallusion Character Creator, AccuRIG, and iClone as optional adapters or source packages, and produces machine-readable evidence without human intervention after registration. |

## Pipeline Format And QA Tool Adapters

Source key prefix: W78:pipeline_format_qa_tool_adapters

Completion rule: every row in this section remains Deferred_Required_Not_Complete until activation gate, implementation artifacts, adapter evidence, preview/overlay evidence, strict QA, and promotion evidence all pass.

| requirement | implementation_target | autonomous_behavior | acceptance_summary |
| --- | --- | --- | --- |
| USD OpenUSD scene package adapter | usd_openusd_scene_package_adapter | Register usd_openusd_scene_package_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | usd_openusd_scene_package_adapter passes only when the autonomous adapter proves the tool can perform long-term scene and asset interchange, supports scene graph, variants, references, asset manifests, and unit/axis conventions, and produces machine-readable evidence without human intervention after registration. |
| Alembic simulation cache adapter | alembic_simulation_cache_adapter | Register alembic_simulation_cache_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | alembic_simulation_cache_adapter passes only when the autonomous adapter proves the tool can perform baked cloth, body, mattress, contact, deformation, and render-pass caches, supports frame count, FPS, units, hashes, and importer proof, and produces machine-readable evidence without human intervention after registration. |
| FFmpeg video audio package adapter | ffmpeg_video_audio_package_adapter | Register ffmpeg_video_audio_package_adapter as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | ffmpeg_video_audio_package_adapter passes only when the autonomous adapter proves the tool can perform video/audio assembly and probe validation, supports codec, duration, frame count, audio stream, AV sync, and hash evidence, and produces machine-readable evidence without human intervention after registration. |
| Geometry and media validator stack | opencv_numpy_pytorch_open3d_trimesh_validator_stack | Register opencv_numpy_pytorch_open3d_trimesh_validator_stack as a controlled backend adapter, not an interactive manual tool. The adapter must expose version/license checks, command templates, smoke tests, input JSON, timeout/watchdog, logs, output manifests, hash manifests, QA evidence, and fallback/blocker behavior. | opencv_numpy_pytorch_open3d_trimesh_validator_stack passes only when the autonomous adapter proves the tool can perform automated geometry, image, video, map, and metric validation, supports OpenCV, NumPy, PyTorch, Open3D, trimesh, mesh metrics, map ranges, overlays, and plots, and produces machine-readable evidence without human intervention after registration. |

