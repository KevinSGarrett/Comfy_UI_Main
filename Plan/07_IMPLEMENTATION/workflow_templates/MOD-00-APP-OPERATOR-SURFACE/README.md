# MOD-00-APP-OPERATOR-SURFACE — App Mode Surface Contract

This is the planned App Mode surface for operator-facing controls.

It must expose high-level controls only. It must not expose secrets, raw model paths, raw LoRA paths, or internal node patch maps.
