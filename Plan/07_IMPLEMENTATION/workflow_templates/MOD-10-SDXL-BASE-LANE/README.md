# MOD-10-SDXL-BASE-LANE — Starter Template Contract

This folder is a Wave 05 starter contract, not a final executable template.

Future implementation must extract the upstream nodes for the current `Main_Flow/SDXL_RealVisXL_LoRA` SaveImage lane into an API workflow template and replace all variable items with patch points.

Required files for full implementation:

- `workflow_api.template.json`
- `patch_points.json`
- `input_contract.json`
- `output_contract.json`
- `qa_contract.json`
