{
  "ec2_sync_targets": [
    "required_workflows",
    "required_models",
    "required_loras",
    "required_reference_assets",
    "required_scripts",
    "expected_outputs",
    "pullback_artifacts"
  ],
  "sync_rule": "sync only cataloged artifacts required by the selected run"
}
