{
  "profiles": [
    {
      "profile_id": "same_plane_no_overlap",
      "rules": [
        "no character may occlude another unless contact is declared"
      ]
    },
    {
      "profile_id": "foreground_background",
      "rules": [
        "foreground instance depth_layer must be lower index than background",
        "occlusion masks required when boxes overlap"
      ]
    },
    {
      "profile_id": "contact_overlap",
      "rules": [
        "source and target contact graph edge required",
        "overlap must be mask-backed"
      ]
    }
  ]
}
