{
  "rules": [
    {
      "if": "contact pressure increases",
      "then": "raise foley transient gain and sharpen attack"
    },
    {
      "if": "contact releases",
      "then": "add release/cloth/body settle sound if material supports it"
    },
    {
      "if": "visual impact is high",
      "then": "impact sound must be present and frame-synced"
    },
    {
      "if": "visual force is low",
      "then": "audio force must remain subtle or be omitted"
    },
    {
      "if": "breathing visual intensity increases",
      "then": "breath audio should increase unless muted by scene policy"
    }
  ]
}
