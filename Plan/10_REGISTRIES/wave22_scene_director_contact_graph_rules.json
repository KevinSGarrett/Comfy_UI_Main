{
  "required_scene_director_outputs": [
    "contact_graph_id",
    "contact_edges",
    "pressure_profiles",
    "occlusion_profiles",
    "audio_force_events",
    "qa_goals"
  ],
  "rules": [
    "Do not create physical contact from prompt text alone without source/target ownership.",
    "Every contact edge must be independently scoreable.",
    "Video/audio contacts must include duration and timing metadata."
  ]
}
