{
  "rules": [
    "Every region mask must resolve to exactly one character_instance_id unless it is a declared contact/occlusion boundary.",
    "Protected identity regions require strict preserve settings.",
    "Hands/fingers must remain owned by their source instance even when touching another character.",
    "Clothing and props must not bridge two characters unless the contact graph declares shared ownership or contact."
  ]
}
