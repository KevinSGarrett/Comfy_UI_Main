{
  "rules": [
    "heavy assets are cataloged by metadata, not stored in repo",
    "every canonical workflow required asset must have an asset catalog entry",
    "assets must declare engine compatibility",
    "rejected or superseded assets must be marked archive_status",
    "missing asset hash is allowed only when explicitly marked hash_pending"
  ]
}
