{
  "rules": [
    "canonical workflows require workflow catalog entry",
    "runtime workflows require source workflow reference",
    "App Mode workflows require control mapping",
    "experiment workflows must not overwrite canonical workflows",
    "archived workflows require replacement metadata"
  ]
}
