{
  "rules": [
    "promotion decisions must link to QA evidence",
    "runtime proof requires output artifact and evidence manifest",
    "preview pass requires preview QA evidence",
    "final render unlock requires preflight evidence",
    "failed evidence should link to rerun plan when available"
  ]
}
