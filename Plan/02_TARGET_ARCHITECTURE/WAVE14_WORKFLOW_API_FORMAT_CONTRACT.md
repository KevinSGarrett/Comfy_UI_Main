# Workflow API Format Contract

The uploaded Main Flow is UI workflow JSON. Runtime submission should use API-format templates.

Wave 14 patches UI workflow JSON for planning and provides contracts for future API-format execution templates.
