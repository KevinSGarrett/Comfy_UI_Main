# Cost / EC2 / No-Touch Boundary

Wave 14 does not start EC2, upload to S3, download models, send SSM commands, or call paid APIs by default.

Cloud/GPU actions require explicit runtime mode, cost guard, and evidence pullback setup.
