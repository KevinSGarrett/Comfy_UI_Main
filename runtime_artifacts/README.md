# runtime_artifacts

Runtime outputs, EC2 pullbacks, run manifests, and review material can be staged here when needed.

Large generated media files should not be committed by default. Before any generated artifact is promoted, it needs matching pullback/hash evidence and QA review records under `Plan\Instructions\QA\Evidence`.
