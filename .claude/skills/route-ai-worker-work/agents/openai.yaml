interface:
  display_name: "Route AI Worker Work"
  short_description: "Route tasks across Codex, Cursor, and Claude"
  default_prompt: "Use $route-ai-worker-work to classify and route this ComfyUI task before starting broad work."
