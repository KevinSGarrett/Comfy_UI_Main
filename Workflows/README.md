# Workflows

Runtime-facing ComfyUI workflow exports live here.

The authoritative source templates are still kept under `Plan\07_IMPLEMENTATION\workflow_templates`. This folder exists so the main project root has directly usable workflow files instead of hiding all operational assets inside planning folders.

Current active base-generation lanes:

- `base_generation\sdxl_low_risk_fallback_lane`
- `base_generation\sdxl_realvisxl_base_lane`
