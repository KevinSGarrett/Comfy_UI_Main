# configs

Safe-to-commit local configuration and handoff files can live here.

Do not place secrets, tokens, AWS credentials, private keys, or machine-local credential exports in this folder. Secrets belong in `.env` or the appropriate local credential store and must stay ignored.
